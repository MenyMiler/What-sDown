import { Box } from '@mui/material';
import { styled } from '@mui/system';

export const DailyEvent = styled(Box)({
    height: '2.9rem',
    display: 'flex',
    alignItems: 'center',
    boxShadow: '0px 3px 15px #00000012',
    borderRadius: '0.625rem',
});
